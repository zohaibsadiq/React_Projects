/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any custom Next.js configuration here
  reactStrictMode: true, // Example setting
  swcMinify: true, // Example setting
};

export default nextConfig;
