import { makeStyles } from '@mui/styles';

export const actorStyles = makeStyles((theme) => ({
  actorImage: {
    width: '90%',
    borderRadius: '20px',
    objectFit: 'cover',
    boxShadow: '0.5em 0.5em 1em',
  },
}));
